import React from 'react';
import s from './nav.module.scss';

const Nav = () => {
    return (
        <div className={s.nav}>
                <h1 className={s.caption}>Gallery</h1>  
        </div>
    );
}

export default Nav;